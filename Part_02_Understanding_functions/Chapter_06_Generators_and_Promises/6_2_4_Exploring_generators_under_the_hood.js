/** 6.2.4 - Exporing generators under the hood */

/**
 * So far we know that calling a generator doesn't execute it. Instead, it creates a new
 * iterator that we can use to request values from the generator.
 */